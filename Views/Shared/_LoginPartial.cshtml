@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    var emails = new string[]{"girnyimykola@gmail.com", "mk0996417892@gmail.com"};
    if(User.HasClaim(item => emails.Contains(item.Value)))
    {
        <a class="text dark" asp-area="" asp-controller="Modules" asp-action="Index">Modules</a>
        <a class="text dark" asp-area="" asp-controller="Lessons" asp-action="Index">Lessons</a>
        <a class="text dark" asp-area="" asp-controller="Exercises" asp-action="Index">Exercises</a>
        <a class="text dark" asp-area="" asp-controller="IOCases" asp-action="Index">Inputs and outputs</a>
    }
    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
        <button type="submit" class="button text dark right">Logout</button>
    </form>
    <a class='text dark right' asp-area="Identity" asp-page="/Account/Manage/Index">@User.Identity?.Name</a>
}
else
{
    <a class='text dark right' asp-area="Identity" asp-page="/Account/Login">Login</a>
    <a class='text dark right' asp-area="Identity" asp-page="/Account/Register">Register</a>
}
